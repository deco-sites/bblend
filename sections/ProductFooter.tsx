import ProductFooter, {
  Props,
} from "$store/components/product/ProductFooter.tsx";

function ProductFooterSection(props: Props) {
  return <ProductFooter {...props} />;
}

export default ProductFooterSection;
