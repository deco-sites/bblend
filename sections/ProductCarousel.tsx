import ProductCarousel, {
  Props,
} from "$store/components/product/ProductCarousel.tsx";

function ProductCarouselSection(props: Props) {
  return <ProductCarousel {...props} />;
}

export default ProductCarouselSection;
